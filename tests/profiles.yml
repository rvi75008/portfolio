# profiles.yml
portfolio:
  outputs:
    dev:
      type: postgres
      host: localhost
      user: dbtdev      # this will be substituted during build time
      pass: password1234  # this will be substituted during build time
      port: 5432
      dbname: dbt
      schema: dev
  target: dev
